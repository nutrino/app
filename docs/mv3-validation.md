# MV3 validation record

검증일: 2026-09-14–15. 대상: `mv3-codex`의 1.8.1 후보 빌드.

## 확인한 결과

- `npm ci --ignore-scripts`: 현재 의존성 집합 설치 성공.
- `npm test`: 27개 자동 테스트 통과. 테스트 전에 두 브라우저용 번들을 생성한다.
- `npm run lint`: 새 실행부의 문법 및 금지된 실행 API 검사 통과.
- Firefox / Chromium 프로덕션 빌드와 ZIP 생성 성공.
- `npm audit`: 알려진 취약점 0개. 이는 악성 코드 부재 또는 무결점 보증이 아니다.
- 커밋 `c3d0b6f`의 [GitHub CI](https://github.com/nutrino/app/actions/runs/34883576306) 성공.
  Windows·macOS·Linux에서 설치·문법 검사·자동 테스트·두 ZIP 생성·의존성 감사 실행.
- macOS 26.4에서 Firefox Developer Edition 156.0b5, Chrome 152.0.7977.83 실행.
- Firefox `xbs-mv3-test` 및 Chrome `xbs-mv3-test` 별도 프로필에서 검증.
  원래 사용자 계정과 북마크는 시험 데이터로 사용하지 않았다.
- 두 브라우저에 MV3 확장 설치, 위성 아이콘 팝업 및 전체 페이지 표시 성공.
- localhost 선택적 호스트 권한 승인 후 가짜 계정 연결·복호화·6개 항목 복원 성공.
- Firefox에서 북마크 추가·제목 수정 → 서버 업로드 → Chrome 수신 성공.
- Chrome에서 같은 북마크 제목 수정 → 서버 업로드 → Firefox 수신 성공.
  원래 항목의 태그·설명도 서버 데이터에서 유지되는 것을 확인했다.
- Chrome에서 Firefox 메뉴가 `[xbs] Menu` 폴더로 변환되는 것을 확인했다.
- Firefox와 Chrome 각각 백업 JSON 다운로드 완료(시험 데이터, 771바이트).
- 두 브라우저 모두 다운로드한 백업 JSON 가져오기 완료. 6개 항목 복원 후
  동기화가 일시 정지되고, 재개 후 서버의 새 데이터를 수신함을 확인했다.
- Firefox 디버깅 화면에서 백그라운드 종료 후 연결·데이터 유지 확인.
- Chrome serviceworker-internals의 STOPPED 상태에서 확장 페이지를 열어
  워커 재기동 및 연결·데이터 유지 확인.
- 두 브라우저에 가짜 항목 76,010개를 실제 복원했다. 진행 중 Firefox의
  Terminate background script와 Chrome의 Stop을 실행한 뒤 진행 위치가
  이어지는 것을 확인했다. Chrome의 새 워커 스레드/DevTools route ID도 확인했다.
- 최종적으로 두 브라우저 모두 ‘동기화 사용 중’, 76,010개를 표시했다.
  이 완료 상태는 실제 native 북마크 전체를 다시 읽어 대상 데이터와 해시를
  비교한 후에만 저장된다. 마지막 항목 `MV3 large 76006` 검색도 양쪽에서 성공했다.
- 시험 후 두 확장을 일시 정지하고 localhost 서버를 종료했다. 가짜 데이터는
  두 테스트 프로필에 남겨 두었으며 원래 사용자 프로필은 교체하지 않았다.
- 시험 API가 HTTP 503을 반환할 때 오류가 표시되고 기존 6개 항목이 유지됨.
  서버를 온라인으로 전환한 후 Chrome의 자동 재시도로 정상 상태 회복 확인.
- 실제 Firefox에서 발견한 fetch 수신 객체 오류를 수정하고 회귀 테스트 추가.

## 자동 테스트의 범위

모의 Firefox/Chromium 북마크 API로 복원, 메뉴 루트 변환, 구분선,
태그·설명 보존, 로컬 수정 업로드, 서버 수정 다운로드를 검증했다.
76,010개 항목을 암호화·복호화·복원하고 최종 데이터 해시를 비교했다.
위 실제 브라우저 시험과 별개로, 자동 테스트에서도 같은 규모의 데이터를 검증한다.

추가로 다음 실패 경로를 검증했다.

- native create 직후 중단 및 체크포인트 직후 중단 시 중복 없이 이어서 복원.
- 서버 PUT 성공 후 응답 유실 시 암호문 비교를 통한 저장 확인.
- 오프라인 편집 후 재접속 업로드.
- 로컬/서버 동시 변경 시 두 사본 보관 및 명시적 해결.
- 서버를 읽는 동안 발생한 로컬 변경 보존.
- 잘못된 암호문·중복 ID·잘못된 URL/응답 검증.
- 백업 복구 후 일시 정지 및 재개 업로드.
- IndexedDB 체크포인트 원자성 및 새 Store 인스턴스에서의 복구.
- 생성된 두 번들의 DOM 없는 환경 시작, 동기 리스너 등록, 재시작 후 메시지 응답.

## 남은 배포·환경 제약

- Windows에서 실제 브라우저 실행은 미검증이다. Windows CI의 빌드·자동 시험은 성공했다.
- Firefox ZIP은 Mozilla 서명 전 빌드다. 임시 설치는 Firefox 종료 시 해제된다.
- 이번 실제 MV3 통합 시험은 localhost 가짜 계정으로 진행했으며,
  운영 서버의 실제 계정으로 새 MV3를 전환하는 작업은 수행하지 않았다.

Firefox 재시작·localhost 권한 및 테스트 북마크 교체에 대한 사용자의 구체적인
승인을 받은 후, 위 두 테스트 프로필에서만 복구·대량 복원 시험을 진행했다.
기존에 자동 승인 검토로 중단했던 백업 가져오기도 이번 승인 후 완료했다.

원래 Firefox 프로필의 실제 북마크는 변경하지 않았다. Firefox 전체 재시작에 따라
원래 MV2의 임시 설치는 해제되었다. 새 MV3는 별도 테스트 프로필에 설치했으며,
실제 서버 계정은 이 검증에 사용하지 않았다. 비밀번호·백업은 Git에 포함하지 않는다.

## 재현용 테스트 서버

```sh
npm run test:server
```

서버는 `127.0.0.1:18765`에만 수신한다. 시험용 ID는
`0123456789abcdef0123456789abcdef`, 시험용 비밀번호는 `test-password-only`다.
이 값은 생성한 가짜 데이터 전용이며 실제 사용자 계정과 무관하다.
`/status`에서 시험용 서버의 복호화 데이터와 쓰기 횟수를 볼 수 있다.
운영 서버를 향하는 URL이나 실제 자격 증명을 이 파일에 넣지 않는다.

서버 터미널의 `offline` / `online` 명령으로 HTTP 503 장애와 복구를 시험할 수 있다.
`large 76010`은 가짜 항목 76,010개, `small`은 초기 6개 항목으로 서버 데이터를
교체한다. 연결된 테스트 확장이 자동으로 내려받으므로, 교체 시험이 승인된
격리 프로필만 연결한 상태에서 실행한다.


## 실제 계정의 Chrome 복원 비교 오류 수정

사용자가 81,462개 실제 계정 복원의 마지막 비교 오류를 보고했다.
확장 내부의 저장된 대상·생성 매핑과 native 북마크를 읽는 진단 기능을 추가하고,
누락·추가·순서·부모·제목·URL·메타데이터 차이를 값 노출 없이 집계했다.
Chrome에서 남은 차이는 URL 한 개였으며, Firefox의 `about:reader?...`가
`chrome://reader/?...`로 저장되는 변환이었다.

Chrome에 한해 이 변환을 인식하면서 원래 동기화 URL을 보존하도록 수정했다.
같이 발견한 제목의 줄바꿈·탭·Unicode 구분자 공백 변환도 원본을 보존한다.
실제 외부 수정이나 다른 URL·쿼리 차이는 계속 오류로 처리한다.

현재 설치를 새로고침한 후 읽기 전용 진단에서 81,462개 전체 일치를 확인했다.
‘동기화 재개’로 마지막 체크포인트의 전체 해시 검사를 완료했고,
‘동기화 사용 중’ 정상 상태가 표시됐다. 북마크 삭제·재생성 없이 복구했다.
서버 저장 시각 표시도 기존 값으로 유지됐다. 운영 계정의 자격 증명과 북마크 원문은
소스·테스트·Git에 포함하지 않았다. 위 초기 시험의 운영 계정 미검증 범위는
이번 실제 복원 오류 복구 결과와 구분한다.

자동 테스트 23개, 문법 검사, Firefox·Chromium ZIP 생성 통과.
새 회귀 테스트는 가짜 읽기 모드 주소와 공백 포함 제목을 사용하며,
진단이 서버/복원 상태를 변경하지 않고 실제 수정은 계속 검출함을 확인한다.

근거: [Chromium about URL 변환](https://chromium.googlesource.com/chromium/src/+/78.0.3904.87/chrome/browser/browser_about_handler.cc),
[Chromium 제목 공백 처리](https://chromium.googlesource.com/chromium/src/+/f96feec0372f945f0430d91b9a93252b1de02855/components/bookmarks/browser/bookmark_node.cc).


## Firefox 기존 북마크 정리 지연 수정

Firefox의 `복원 중 (0/81459)` 보고를 확인했다. 새 항목을 생성하기 전에
기존 시험 북마크 76,007개를 정리하는 중이었으나 화면은 생성 진행률만 표시했다.
같은 폴더의 앞 항목부터 삭제하던 순서를 뒤에서부터 삭제하도록 바꾸고,
8개씩 완료를 기다린 뒤 체크포인트를 저장하도록 개선했다. 중단된 이전 빌드의
계획은 남은 ID만 재배열하며 원래 백업과 대상 데이터는 유지한다.
삭제 도중 종료돼도 이미 사라진 ID를 건너뛰며 이어가는 회귀 시험을 추가했다.

화면에 ‘기존 북마크 정리 중’의 실제 진행률을 표시한다. IndexedDB 복원 기록은
getAllKeys/getAll 두 요청으로 읽고 완료 기록은 IDBKeyRange로 일괄 정리한다.
한 행마다 이벤트가 발생하는 커서 방식의 반복 비용을 줄였다. 서로 다른 복원의
기록 및 백업이 유지되는 것을 자동 시험으로 검증했다.

Firefox에 수정 빌드를 로드하여 정리가 26,178/76,007개까지 진행되는 것을 확인했다.
Firefox 자체 북마크 Sync는 꺼져 있었다. 설치된 Bookmark Info의 공식 최신 배포
2.26.4 소스에서는 새 북마크를 index 0으로 이동하고 새 폴더도 재정렬하는 동작을
확인했다. 당시에는 간섭 가능성을 우려하여 생성 단계 진입 전에 복원을 일시 정지했다.
이는 설치된 버전에서 실제 간섭을 재현한 결과는 아니며, 다른 확장의 중지를 복원
재개의 필수 조건으로 판단할 근거는 부족했다. 아래 후속 검증에서는 설정을 그대로
둔 채 전체 복원을 완료했다.
자동 테스트 27개, 문법 검사와 두 브라우저 패키지 생성은 통과했다.


추가로 긴 복원 도중 알람이 중복 실행 요청을 대기열에 쌓아 일시 정지가 늦어지는
문제를 수정했다. 진행 중인 run Promise를 공유하여 겹치는 요청을 합친다.
두 브라우저의 실제 번들 시험에서 50개 알람을 겹쳐도 native 조회가 한 번만
실행되고 일시 정지가 완료되는 것을 검증했다. 현재 Firefox에서도 수정 빌드로
다시 로드한 후 일시 정지를 눌러 현재 청크 종료 후 ‘동기화 재개’ 버튼으로
바뀌고 진행 위치가 보존되는 것을 확인했다. 원래 백업과 서버 복원 대상은 유지한다.

## Firefox 실제 계정 복원 완료 — 2026-09-15

사용자의 계속 진행 요청에 따라 QuickBookmarkToFolder와 Bookmark Info 등 다른
확장의 설정을 변경하지 않고 기존 체크포인트에서 복원을 재개했다.
QuickBookmarkToFolder는 사용자가 현재 페이지를 북마크에 추가하는 확장으로,
다른 북마크 동기화 확장으로 취급하지 않는다.

기존 북마크 정리 76,007개를 마친 뒤 81,459개 생성이 끝나고, 기본 루트 3개를 포함한
총 81,462개 전체 해시 비교가 통과했다. Firefox 화면이 ‘동기화 사용 중’으로
전환됐으며, 서버의 마지막 저장 시각은 복원 전과 동일하게 유지됐다.
이 환경에서 다른 확장의 간섭으로 인한 복원 불일치는 재현되지 않았다.
Chrome에서도 같은 81,462개 항목과 ‘동기화 사용 중’ 상태를 확인했다.
두 브라우저에서 ‘지금 동기화’를 실행한 후에도 오류와 서버 저장 시각 변경 없이
정상 상태를 유지하는 것을 확인했다. 이번 추가 검증에서는 북마크 내용이나 다른
확장의 설정을 임의로 변경하지 않았다.

## 최초 연결 권한과 입력값 저장 개선 — 2026-09-15

이전 화면은 연결 제출 시 권한을 요청하여 팝업이 닫히면 후속 연결이 끊길 수 있었고,
URL 자동완성을 사용하면서 입력값을 자체 저장하지 않았다. 최초 설치와 미연결
팝업 진입 시 설정 탭을 열고, 서버 주소에 대한 권한을 계정 입력보다 먼저 요청한다.
권한 승인 후에는 해당 탭에서 한 번의 연결 제출로 서버 데이터를 확인한다.
새 서버로 변경하거나 권한이 취소된 경우 다시 권한 단계로 전환한다.

주소와 ID는 storage.local, 비밀번호는 storage.session에 저장한다. 입력 이벤트마다
저장하고 빠른 입력의 저장 순서를 보장하며, 화면 초기화 시 저장값을 복원한다.
브라우저 URL 자동완성을 끄고 전용 필드 이름을 사용한다. 특정 CGV 주소의 출처를
직접 재현한 것은 아니며, 소스에 해당 주소를 기본값으로 저장하는 코드는 없었다.
비밀번호는 사용자 선택에 따라 브라우저 재시작 후에는 복원하지 않는다.

Firefox Developer Edition 157의 별도 xbs-setup-test 프로필과 Chrome에서 테스트용
임시 확장을 설치하여 설정 탭 자동 열림, localhost 한정 권한 요청, 권한 승인 직후
계정 입력 단계 전환, 새로고침 후 주소·ID·비밀번호 유지, 단 한 번의 연결 제출로
가짜 계정의 6개 항목 미리보기 도달을 확인했다. 복원 버튼은 누르지 않았으며,
테스트 확장을 제거하고 로컬 테스트 서버를 종료했다.

자동 테스트 32개, 문법 검사, Firefox·Chrome 배포용 ZIP 생성 통과. 추가 시험은
권한 거절·취소, 연결 실패 시 입력 보존, 세션 종료 시 비밀번호만 초기화,
이전 권한 조회 응답의 역전, 기존 연결 주소 이전, 설치 때만 설정 탭 열기를 검증한다.

권한 요청은 [브라우저의 사용자 동작 요건](https://developer.mozilla.org/en-US/docs/Mozilla/Add-ons/WebExtensions/API/permissions/request)에
따라 초기 설정 화면의 버튼에서 수행한다. 주소를 입력하기 전 모든 사이트의 권한을
한꺼번에 요청하지 않는다.

## Windows Firefox의 순서 88개 차이 보고 대응

사용자가 81,462개 복원 마지막 검사에서 ‘순서 88개’ 차이를 보고했다. 이 보고는
내용과 부모가 같은 항목의 형제 순서가 다르다는 의미이며, Windows 자체나 특정
확장이 원인이라고 확정할 자료는 없다. 기존 생성 코드는 각 create 완료를 기다리지만,
완료 시 순서가 달라진 경우를 복구하지 않고 중단했다.

전체 비교 결과가 순서 차이만 있을 때 기존 생성 매핑과 계획으로 해당 폴더의
순서를 재조정한다. native move를 한 번씩 기다리며 동일 부모 내에서만 이동한다.
폴더의 실제 ID 집합도 이동 직전에 검사한다. 진행 기록은 이동 전에 저장하여
응답 유실이나 중단 후에도 실제 순서를 다시 읽어 이어간다. 폴더 중간에도 시간
예산에 따라 반환하며, 다음 청크에서 전체 내용 비교를 다시 수행한다.
두 차례 조정 및 이동 횟수 상한으로 지속적인 외부 변경에 따른 무한 반복을 막는다.
최종 전체 해시 비교는 유지하고, 기존 백업과 서버 내용은 변경하지 않는다.

수동 재개 시 오래된 오류를 지워 정상 청크의 후속 실행이 지연되지 않도록 했다.
수동 재개는 순서 복구의 재시도 한도도 새로 부여하되 진행 위치는 보존한다.

Firefox·Chrome 모델에서 88개 순서 차이를 재현하여 재생성·업로드 없이 복구함을
검증했다. 이동 직후 응답 유실, 폴더 중간 중단 및 새 Engine에서 재개, 내용 차이
동반 시 보존, 검사 후 폴더 구성 변경, 계속되는 재정렬의 제한 종료, Chrome의
Other 안 Menu 래퍼 순서도 시험했다. 자동 테스트 40개, 문법 검사 및 두 ZIP 빌드 통과.
Windows 11 실제 브라우저에는 여기서 접근할 수 없으므로 사용자 환경의 원인 재현과
수정본 적용 후 완료 여부는 아직 확인하지 않았다.

API 근거: [Mozilla bookmarks.move](https://developer.mozilla.org/en-US/docs/Mozilla/Add-ons/WebExtensions/API/bookmarks/move).

## 적용 빌드 식별 및 실행부 비교

이후 사용자는 재개 후 다시 멈춘 상황에서 실제로 최신 버전을 반영하지 않았다고
확인했다. 최신 순서 복구 코드가 실행됐는데도 같은 오류가 재발한 것으로 취급하지 않는다.

빌드 시 Git HEAD 커밋 해시·committer 시각, 미커밋 변경 여부, 런타임 소스와 빌드
입력의 SHA-256 지문, 빌드 시각을 화면과 실행부 번들에 동일하게 포함한다.
배포 파일을 덮어써도 실행 중인 코드의 정보가 바뀌지 않도록 정적 JSON을 실행 시
읽는 방식 대신 번들 상수로 삽입한다. build-info.json도 ZIP에 포함하여 배포본을
따로 확인할 수 있다. Git 정보가 없는 압축본도 정보 확인 불가를 명시하며 빌드된다.

화면은 로드 직후 자기 정보를 표시하고, 1.5초 상태 갱신마다 실제 실행부 정보를
받아 비교한다. 커밋·소스 지문·빌드 시각·플랫폼 중 하나라도 다르거나 구버전
실행부라 정보가 없으면 재로드 안내를 표시한다. 복원 단계·생성 위치·순서 조정
시도와 이동 횟수도 표시한다. 서버 자격 증명이나 북마크 내용은 포함하지 않는다.

자동 테스트 41개와 문법 검사를 통과했다. 두 실제 번들의 status 응답을 생성된
build-info.json과 비교하고, 화면 번들의 동일 지문·시각 포함, 빌드 불일치 및
메타데이터 미제공 시 표시도 검증했다.

## 최초 동기화 방향 및 차이만 적용 — 2026-09-16

서버 자료가 있는 최초 미리보기의 실행은 전체 삭제 계획 대신 로컬 항목을 대응시키는
계획을 사용한다. 같은 부모 아래 폴더 이름·북마크 URL로 대응시키고 동일 제목을
우선한다. 중복 후보는 일대일로 소비하여 중복 수를 보존한다. 일치한 항목은 native ID를
유지하고 제목 수정만 필요하면 update한다. 서버에 없는 항목만 제거하며, 새 항목은
남은 형제의 끝에 추가하고 기존 순서 복구 단계에서 최종 서버 순서로 맞춘다.
append 위치를 미리 기록하여 생성 응답 유실 시 기존 형제와 혼동하지 않고 재개한다.
다른 부모로 이동했거나 이름이 달라진 폴더 등 대응 불가능한 부분은 교체할 수 있다.

최초 서버가 빈 트리 또는 유효한 lastUpdated를 가진 미초기화 응답이면 로컬을
보존하여 최초 업로드를 준비한다. 명시적으로 실행하기 전에는 업로드하지 않는다.
기존 pendingUpload 확인 절차를 재사용하여 PUT 응답 유실 후 중복 저장을 피한다.
미리보기 이후 빈 상태가 바뀌면 방향을 재확인하도록 중단한다. 잘못된 암호문이나
비정상 응답을 빈 계정으로 취급하지 않는다. ID 생성 자체는 추가하지 않았다.
이 분기는 최초 적용에만 사용하며 일반 양방향 동기화와 백업 복구 흐름은 유지한다.

자동 테스트 53개와 문법 검사를 통과했다. Firefox·Chrome의 동일 로컬에서 native
생성·수정·이동·삭제가 모두 0임을 확인했고, 기존 76,010개 대량 데이터 시험에도
동일 자료의 최초 재연결 무변경 검증을 추가했다. 부분 변경에서는 1개 추가·1개 수정·
1개 삭제와 기존 ID 보존, 중복 URL, 생성 응답 유실 후 재개, 빈 서버 업로드 응답 유실,
미리보기 방향 변경, 미초기화 응답 검증을 시험했다. 실제 사용자의 로컬/서버 자료를
변경하는 시험은 수행하지 않았다.

Chrome에서 Menu가 비어 있고 실제 래퍼 폴더가 없으면 최초 차이 적용 시 이를
불필요하게 만들지 않는 시험도 통과했다.

## 서버 주소 자동완성 재활성화 — 2026-09-16

사용자 요청으로 서버 주소 입력란의 autocomplete를 url로 변경했다. 주소·ID의
확장 내부 자동 저장과 복원, 비밀번호의 세션 한정 저장은 유지한다. ID·비밀번호
입력란의 자동완성 설정은 변경하지 않았다. 이전 자동완성 비활성화 결정을 대체한다.

## 서버 목록과 동기화 방향 선택 — 2026-09-16

기존 Angular 검색 화면은 ID 내림차순의 최근 목록과 폴더 탐색을 제공했지만,
MV3 화면에는 로컬 bookmarks.search만 이식되어 빈 검색의 목록도 표시하지 않았다.
Manifest V3의 제한이 아니라 화면 재작성 중 누락된 기능이다.

서버에서 읽어 복호화한 자료로 최근 목록과 폴더 탐색을 추가했다. 최근 순은 기존과
동일한 ID 내림차순이며 실제 추가 날짜가 아니라는 점을 화면에 명시한다. 폴더의 순서와
경로를 유지하고 100개씩 표시한다. 제목·주소·설명·태그 검색, 서버 새로 읽기를 제공한다.
충돌·정지·최초 미리보기 상태에서도 로컬이나 서버를 변경하지 않고 조회한다.
특수 URL은 실행 링크로 만들지 않고 텍스트로 표시한다.

방향은 최초 연결과 이후 모두 서버 → 로컬, 로컬 → 서버, 양방향 중 선택해 저장한다.
기존 연결은 양방향을 유지한다. 새 연결은 서버가 비어 있으면 업로드, 자료가 있으면
다운로드를 기본 선택한다. 단방향 설정은 자동 동기화에도 유지된다. 다운로드는
기존 네이티브 ID를 최대한 유지하고 차이만 적용한다. 업로드는 서버 교체 전 사본을
별도 보관하며 다운로드할 수 있다. 진행 중인 복원 또는 미확정 업로드가 있으면 방향
변경과 계정 해제를 차단한다. 미리보기 이후 서버의 빈 상태가 바뀌면 재확인을 요구한다.

최초 양방향은 공통 부모의 제목·URL이 같은 항목을 일대일 대응시킨다. 서버 순서와
메타데이터를 유지하고 로컬 전용 항목에 새 ID를 부여해 추가한다. 같은 이름의 폴더는
재귀적으로 합친다. 로컬 적용 및 전체 해시 검증을 마친 후 조건부 업로드하며, 그 사이
서버가 바뀌면 충돌로 중단한다. 이후 양방향의 기존 충돌 감지 정책은 유지한다.

자동 테스트 70개 및 MV3 문법·런타임 정책 검사를 통과했다. Firefox/Chrome 네이티브
API 모형으로 방향별 처리, 빈 서버, 중복 병합, 서버 백업, 업로드 응답 유실, 재시작 시
방향 유지와 동시 수정 충돌을 검증했다. 두 플랫폼의 생성된 app.js를 모형 DOM에서
실행하여 방향 저장·폴더 진입·경로·최근 보기 전환 및 특수 URL 비실행을 확인했다.
실제 사용자 서버의 자료를 변경하거나 Windows 브라우저에서 이 변경을 실행한 검증은
이번 작업에 포함하지 않았다.

## Windows Edge 복원 URL 불일치 진단 — 2026-09-16

보고된 `url 1개, 순서 13개`는 URL 내용 차이가 존재해 순서 전용 보정 조건을 충족하지
못한 상태다. 오류에 원본/실제 URL이 없어 Edge 주소 변환인지 외부 수정인지 확정할 수
없으며, 검증을 완화하거나 추정으로 URL 동등성 범위를 넓히지 않았다.

복원 중 `URL 차이 상세 확인` 버튼으로 불일치 항목의 서버 동기화 ID, 로컬 북마크 ID,
대상 URL과 실제 스냅샷 URL을 최대 20개 표시한다. 기본 오류·기존 진단에는 계속 URL을
노출하지 않는다. 상세 진단은 사용자가 해당 버튼을 눌렀을 때만 표시하고 네트워크로
전송하지 않는다. 재개/삭제/서버 쓰기 없이 중단된 복원 상태를 조사할 수 있다.
회귀 테스트는 기본 진단의 URL 비노출, 상세 ID·URL 출력, 상태·북마크 불변을 확인한다.
실제 Windows 항목 ID 81507의 URL 쌍 확인 후 호환성 수정 여부를 결정해야 한다.

## Edge about:reader 변환 호환성 수정 — 2026-09-16

사용자가 제공한 상세 진단에서 ID 81507의 대상 `about:reader?url=…`이 Edge에
`edge://reader/?url=…`로 저장되는 것을 확인했다. 기존 코드는 Chrome의
`chrome://reader/?url=…`만 인식하여 URL 1개 차이로 중단했고 순서 보정도 실행되지 않았다.

Chromium URL 비교·최초 항목 대응·최초 양방향 병합 키에 Edge reader 별칭을 반영했다.
원본 서버 URL은 보존하며 경로·쿼리·fragment의 실제 차이는 계속 검출한다. Edge의
다른 내부 URL 전체를 동일하다고 취급하지 않으며 Firefox에는 적용하지 않는다.

회귀 테스트에서 이전 버전의 URL 불일치와 순서 불일치를 함께 만든 뒤 저장된 복원
기록으로 실행부를 재시작했다. 재개 후 create/update/removeTree 없이 순서 보정 및
전체 해시 검증을 완료했고 서버 쓰기가 없음을 확인했다. 별도 테스트로 쿼리·fragment·
경로 변경, 인증 정보 및 about:blank/srcdoc 오인식을 차단함을 검증했다.
실제 Windows Edge 실행은 사용자 환경에서 수정 빌드 재로드 후 재개로 확인해야 한다.

## 팝업 로컬 폴더 검색과 현재 페이지 추가 — 2026-09-16

Quick Bookmark To Folder의 폴더 검색·선택 흐름을 참고하여 자체 구현했다.
팝업 첫 섹션에서 폴더 이름과 전체 경로를 검색하고 경로 이름순으로 표시한다.
50개 단위 더 보기, 전체 검색 개수, 현재 페이지 제목, 저장 성공/중복 안내를 제공한다.
브라우저의 가상 루트와 관리자가 잠근 폴더 하위는 저장 대상에서 제외한다.

activeTab만 추가해 아이콘을 누른 현재 탭의 제목·주소를 얻는다. 서버 미연결 팝업도
유지하며 서버 권한 요청은 별도의 설정 탭에서 수행한다. 확장 페이지 자체는 저장하지
않고, javascript/data URL의 빠른 저장은 거부한다. 로컬 추가는 백그라운드 엔진의
복원/동기화와 같은 직렬 실행 경로로 처리한다. 복원 중 추가와 서버 → 로컬 자동 실행
중 추가를 차단해 복원 검증 실패 또는 즉시 삭제를 방지한다. 같은 폴더의 동일 URL은
중복 생성하지 않으며 폴더 존재와 쓰기 가능 여부는 저장 직전에 다시 확인한다.

테스트 77개 통과: 두 브라우저용 app.js 모형 DOM에서 현재 탭 읽기, 폴더 검색과 클릭,
정확한 부모 ID·페이지 URL 전달 및 저장 메시지를 검증했다. 네이티브 API 모형으로
Firefox/Chromium 폴더 저장, 중복 방지, 잘못된 대상 거부, 복원·다운로드 모드 보호를
확인했다. 경로 검색·대소문자/전각 정규화·동일 폴더명·관리 폴더 제외도 검증했다.
이번 변경의 실제 브라우저 팝업 렌더링은 자동 테스트 범위에 포함하지 않았다.

## 최근 저장 폴더 10개와 서버 목록 30개 — 2026-09-16

빠른 추가에서 실제 저장에 성공한 폴더 ID를 로컬 IndexedDB에 최신순으로 최대 10개
보관한다. 재사용한 폴더는 맨 앞으로 이동하고 중복은 제거한다. 중복 북마크로 저장을
생략했거나 저장에 실패하면 순서를 바꾸지 않는다. 다시 열 때 현재 폴더의 이름·경로를
사용하며 삭제되거나 관리 대상이 된 폴더는 제외한다. 최근 폴더에서도 바로 저장할 수 있다.
기존 버전에는 사용 이력이 없어 업데이트 이후 저장부터 최근 목록이 채워진다.

서버 최근 북마크와 폴더 보기를 30개 단위로 변경하고 이전/다음 이동도 응답의 페이지
크기를 사용하도록 바꿨다. 로컬 전체 폴더 검색의 50개씩 더 보기는 유지한다.
테스트 78개 통과: 최신순·10개 제한·중복 제거·재시작 보존·삭제 제외·저장 실패 시 불변,
팝업 저장 직후 목록 갱신 및 서버 30개 페이지 경계를 검증했다.

## 연결 정보 쿠키와 입력 대기 검색 수정 — 2026-09-17

주소·ID를 전용 예약 도메인 `https://settings.xbrowsersync.invalid/`의 쿠키에 저장한다.
1년 만료, Secure·HttpOnly·SameSite=Strict이며 비밀번호·암호화 키는 제외한다. 이 주소에
fetch/탭 열기 등 네트워크 요청을 하지 않는다. 일반 웹 쿠키 저장소를 사용하므로 확장
로컬 저장소가 없는 재설치에서 입력값 복원에 사용한다. 쿠키 삭제/다른 프로필/만료에는
복원할 수 없다. 현재 연결 및 로컬 초안이 있으면 우선하며 쿠키 읽기·쓰기 실패는 로컬
저장을 막지 않고 표시한다. 서버 권한 승인과 연결은 자동 수행하지 않는다.

한글 IME가 마지막 글자를 조합 상태로 유지하면 이전 코드가 타이머를 예약하지 않던
문제를 수정했다. 조합 여부와 관계없이 마지막 입력 후 1초 동안 멈추면 현재 입력값으로
검색한다. 입력 대기/빈 검색 안내 문구는 제거하고 결과가 나왔을 때만 개수를 표시한다.
현재 탭 URL을 읽지 못하거나 설정 탭인 경우에도 폴더를 읽도록 수정했다.

테스트 82개 통과. Firefox/Chromium 번들에서 IME 조합 유지 중 999ms/1000ms 경계를
검증했다. 로컬 저장소 초기화 후 쿠키 복원, 비밀번호 제외, 쿠키 실패 시 로컬 저장,
현재 탭 조회 실패에도 폴더 로드가 수행됨을 API 모형으로 확인했다.
실제 브라우저의 쿠키 정책·확장 제거 및 재설치에 대한 실기 검증은 아직 수행하지 않았다.

## 팝업 초기화 지연과 서버 호출 감소 — 2026-09-17

검색 입력 지연을 500ms로 변경했다. 팝업 폴더 조회가 Engine.exclusive의 네트워크 동기화
뒤에 줄을 서던 구조를 제거했다. 조회는 읽기 전용으로 독립 실행하며, 실행부 메모리의
폴더 인덱스 Promise를 공유해 동시 요청과 재오픈 시 중복 getTree/정렬을 줄인다.
북마크 변경 이벤트와 수동 새로 읽기에서 캐시를 무효화한다. 저장 작업은 기존처럼
직렬 실행하고 저장 직전 대상 폴더와 복원 상태를 검증한다.

앱 초기화는 현재 탭·폴더 조회를 즉시 시작하고 상태·연결 폼 로드를 병렬화한다. 첫 상태
응답을 재사용해 초기 status 중복 요청을 제거했고, 로컬 초안과 쿠키가 같으면 다시 쓰지
않는다. 정렬용 Intl.Collator와 강조 표시용 Intl.Segmenter도 재사용한다.

실행부 최상위 schedule(0)을 제거해 팝업 때문에 서비스 워커가 깨어날 때 서버 조회가
시작되지 않도록 했다. 기존 알람은 다시 생성하지 않아 팝업을 열 때 주기가 초기화되지
않는다. 설치·브라우저 시작·명시적 동기화·북마크 변경 및 1분 주기 알람은 유지한다.
중간에 실행부가 종료된 복원/업로드는 다음 알람 또는 명시적 재개로 계속 처리된다.

테스트 84개 통과. 499ms/500ms 검색 경계, 실행부 부팅 시 즉시 동기화 예약 없음,
동기화 대기열이 막혀도 폴더 조회 완료, 동시/반복 조회의 getTree 1회 공유, 변경 후
캐시 무효화, 수동 새로 읽기 및 실패 후 재시도를 검증했다. 사용자가 보고한 사이트별
3초 지연의 실측 재현 및 실제 Windows 브라우저의 전후 시간 측정은 하지 않았다.
